name: Deploy
run-name: ${{ github.event.head_commit.message }}

on:
  push:
    branches: [main]

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - name: Pushed
        run: echo "Deployed ${{ github.event.head_commit.message }}"
